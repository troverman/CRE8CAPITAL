{
  "name": "cre8capital-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "CRE8 Capital frontend and backend workspace",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "backend": "npm --prefix backend run start",
    "backend:api": "npm --prefix backend run serve"
  }
}
